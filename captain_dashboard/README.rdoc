# Captain Dashboard
## Requirements
You need a running etcd server. This command spins up an etcd server in a docker container:
```
docker run -d --name etcd -p 4001:4001 quay.io/coreos/etcd:v0.4.6 -name test -addr 127.0.0.1:4001
```
