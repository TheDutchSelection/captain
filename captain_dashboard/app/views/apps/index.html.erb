<%= content_for :title, "#{t('app.name')} - #{t('defaults.apps')}" %>
<div class="col-xs-12">
  <div class="page-header">
    <h1 class="pull-left">
      <%= t('defaults.apps') %>
    </h1>
    <div class="buttons pull-right">
      <div class="btn-group" role="group">
        <%= link_to t('views.apps.index.new'), new_app_path, class:'btn btn-default' %>
      </div>
    </div>
  </div>
  <div class="row">
    <div class="col-xs-12">
      <%= render partial: 'app_table' %>
    </div>
  </div>
</div>

