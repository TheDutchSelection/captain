<%= content_for :title, "#{t('app.name')} - #{t('defaults.users')}" %>
<div class="col-xs-12">
  <div class="page-header">
    <h1 class="pull-left">
      <%= t('default.users') %>
    </h1>
    <div class="buttons pull-right">
      <div class="btn-group" role="group">
        <%= link_to t('views.users.index.new'), new_user_path, class:'btn btn-default' %>
      </div>
    </div>
  </div>
  <div class="row">
    <div class="col-xs-12">
      <%= render partial: 'user_table' %>
    </div>
  </div>
</div>

